{
  "name": "clone-x-capstone-1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "api": "json-server --watch db.json --port 3000",
    "web": "serve . -l 3001"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "serve": "^14.2.4"
  }
}
